# Tailscale hostname for remote testing over the tailnet.
# Find yours with: `tailscale status` (it's the MagicDNS name of this machine).
# Leave unset for local-only development.
TAILSCALE_HOST=your-machine.tail-name.ts.net
