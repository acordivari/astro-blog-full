---
// BirdsOfPreyMap — birds of prey sightings around a location, from the GBIF
// Occurrence API and rendered with Leaflet.js.
//
// A page can hold several of these, each with its own location, cache and
// species list, so every element inside the widget is addressed by class and
// looked up within the instance root rather than by a page-wide id. The species
// detail overlay is the one shared piece: render <BirdsSpeciesModal /> exactly
// once on any page that uses this component.
interface Props {
  lat: number;
  lng: number;
  place: string;
  radius?: number;
  /** URL of a dataset baked by `npm run birds:refresh`, e.g.
   *  "/data/birds/boulder-co.json". When present the default view loads from it
   *  instead of querying GBIF, which is the difference between one CDN request
   *  and a couple of dozen slow API calls. Searching an address still goes live. */
  seed?: string;
}

const { lat, lng, place, radius = 100, seed } = Astro.props;

// Deterministic per-instance prefix so the radius <label for> points at its own
// slider rather than the first one on the page.
const uid = `birds-${place.toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/^-|-$/g, '')}`;
---

<div
  class="birds-map-container"
  data-birds-map
  data-lat={lat}
  data-lng={lng}
  data-place={place}
  data-radius={radius}
  data-seed={seed}
>
  <div class="birds-header">
    <h2 class="birds-heading">Birds of Prey Near {place}</h2>
    <p class="birds-subtitle">Recent sightings within {radius} miles of {place} — from GBIF</p>
  </div>

  <form class="birds-search birds-search-form">
    <input
      type="text"
      class="birds-address-input"
      placeholder="Enter an address, city, or place — e.g. Denver, CO"
      autocomplete="off"
      aria-label={`Address or place to search for birds of prey, replacing ${place}`}
    />
    <button type="submit" class="birds-search-btn">Search</button>
  </form>

  <div class="birds-radius">
    <label for={`${uid}-radius`}>
      Search radius: <span class="birds-radius-value">{radius}</span> miles
    </label>
    <input
      type="range"
      id={`${uid}-radius`}
      class="birds-radius-slider"
      min="10"
      max="150"
      step="5"
      value={radius}
    />
  </div>

  <div class="birds-place"></div>

  <div class="birds-controls">
    <select class="birds-taxon" aria-label={`Filter birds of prey near ${place} by group`}>
      <option value="all">All Birds of Prey</option>
      <option value="Accipitriformes">Hawks, Eagles &amp; Kites (Accipitriformes)</option>
      <option value="Falconiformes">Falcons (Falconiformes)</option>
      <option value="Strigiformes">Owls (Strigiformes)</option>
    </select>
    <button class="birds-refresh">Refresh Data</button>
  </div>

  <div class="birds-message birds-loading">Loading bird sightings...</div>
  <div class="birds-message birds-error" style="display:none"></div>

  <div class="birds-map"></div>

  <div class="birds-insights" style="display:none"></div>

  <div class="birds-species-section">
    <h3>Species Found <span class="birds-species-count-label"></span></h3>
    <p class="birds-section-subtitle">Click a species for photos and details</p>
    <div class="birds-species-grid"></div>
  </div>

  <div class="birds-stats">
    <span class="birds-count">0 sightings</span>
    <span class="birds-species-count">0 species</span>
    <span class="birds-sample-note"></span>
    <span class="cache-status birds-cache-status"></span>
  </div>

  <p class="birds-attribution">
    Data from <a href="https://www.gbif.org" target="_blank" rel="noopener">GBIF.org</a> |
    Photos from <a href="https://www.inaturalist.org" target="_blank" rel="noopener">iNaturalist</a> |
    Descriptions from <a href="https://www.wikipedia.org" target="_blank" rel="noopener">Wikipedia</a>
  </p>
</div>

<!-- Leaflet CSS -->
<link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css" />

<script>
  import { getRaptorInfo, WINGSPAN_REFERENCES, WINGSPAN_AXIS_MAX_CM } from '../data/raptors';
  import type { RaptorInfo } from '../data/raptors';
  import { fetchLocationData, enrichVernacularNames, geocodeAddress } from '../lib/gbif.js';

  /*
   * This script is hoisted by Astro and runs once per page, no matter how many
   * <BirdsOfPreyMap /> instances are on it. Everything below the "Per-instance
   * widget" divider is created fresh for each map root; everything above it —
   * the Leaflet loader, the IndexedDB cache, the GBIF request chain and the
   * species modal — is shared by all of them.
   */

  const MILES_TO_KM = 1.60934;

  // Leaflet paints to canvas/SVG, so it cannot read CSS variables itself.
  // Pull the map palette off :root once so the layers follow the active theme.
  const mapToken = (name: string, fallback: string) =>
    getComputedStyle(document.documentElement).getPropertyValue(name).trim() || fallback;

  // Cache configuration - using IndexedDB for larger storage
  const DB_NAME = 'birds-of-prey-db';

  const DB_VERSION = 1;

  const STORE_NAME = 'cache';

  interface CachedData {
    id: string;
    timestamp: number;
    payload: LocationPayload;
  }

  // What both the baked JSON in public/data/birds/ and a live search produce.
  // `species` counts are exact (facet-derived, whole dataset); `occurrences` is
  // only a sample, enough to draw dots and a seasonality histogram.
  interface LocationPayload {
    version: number;
    fetchedAt: string;
    place: string;
    lat: number;
    lng: number;
    radiusMiles: number;
    species: SpeciesRow[];
    occurrences: { y: number; x: number; k: number; d: string; s: string }[];
  }

  interface SpeciesRow {
    key: number;
    name: string;
    vernacular: string;
    order: string;
    family: string;
    count: number;
  }

  // Species data for current view
  // Derived per-render view of SpeciesRow, keyed by scientific name.
  interface SpeciesInfo {
    scientificName: string;
    vernacularName: string;
    order: string;
    family: string;
    count: number;
    gbifKey: number;
  }

  // Leaflet is loaded once and shared; every map awaits the same promise.
  let leafletPromise: Promise<any> | null = null;

  function loadLeaflet(): Promise<any> {
    if ((window as any).L) return Promise.resolve((window as any).L);
    if (!leafletPromise) {
      leafletPromise = new Promise((resolve, reject) => {
        const script = document.createElement('script');
        script.src = 'https://unpkg.com/leaflet@1.9.4/dist/leaflet.js';
        script.onload = () => resolve((window as any).L);
        script.onerror = () => reject(new Error('Failed to load Leaflet'));
        document.head.appendChild(script);
      });
    }
    return leafletPromise;
  }

  // Escape a value destined for an HTML attribute in a popup template.
  function escapeAttr(value: string): string {
    return value.replace(/&/g, '&amp;').replace(/"/g, '&quot;').replace(/</g, '&lt;');
  }

  // IndexedDB helper functions
  function openDB(): Promise<IDBDatabase> {
    return new Promise((resolve, reject) => {
      const request = indexedDB.open(DB_NAME, DB_VERSION);
      request.onerror = () => reject(request.error);
      request.onsuccess = () => resolve(request.result);
      request.onupgradeneeded = (event) => {
        const db = (event.target as IDBOpenDBRequest).result;
        if (!db.objectStoreNames.contains(STORE_NAME)) {
          db.createObjectStore(STORE_NAME, { keyPath: 'id' });
        }
      };
    });
  }

  // Cache is keyed per location + radius so different searches don't clobber each other.
  function cacheKey(lat: number, lng: number, radiusMiles: number): string {
    return `birds:v3:${lat.toFixed(2)}:${lng.toFixed(2)}:${radiusMiles}`;
  }

  async function loadFromCache(key: string): Promise<CachedData | null> {
    try {
      const db = await openDB();
      return new Promise((resolve, reject) => {
        const tx = db.transaction(STORE_NAME, 'readonly');
        const store = tx.objectStore(STORE_NAME);
        const request = store.get(key);
        request.onerror = () => reject(request.error);
        request.onsuccess = () => {
          const data = request.result as CachedData | undefined;
          if (data) {
            console.log('Loaded from IndexedDB:', key, data.payload?.species.length, 'species');
          } else {
            console.log('No cache in IndexedDB for', key);
          }
          resolve(data || null);
        };
      });
    } catch (e) {
      console.error('Error loading cache:', e);
      return null;
    }
  }

  async function saveToCache(key: string, payload: LocationPayload): Promise<number | null> {
    try {
      const db = await openDB();
      const timestamp = Date.now();
      const data: CachedData = { id: key, timestamp, payload };

      return new Promise<number>((resolve, reject) => {
        const tx = db.transaction(STORE_NAME, 'readwrite');
        const store = tx.objectStore(STORE_NAME);
        const request = store.put(data);
        request.onerror = () => reject(request.error);
        request.onsuccess = () => {
          console.log('Saved to IndexedDB:', payload.species.length, 'species,', payload.occurrences.length, 'sampled dots');
          resolve(timestamp);
        };
      });
    } catch (e) {
      console.warn('Failed to save to cache:', e);
      return null;
    }
  }

  // Trim Nominatim's long display_name to something heading-friendly.
  function shortPlaceName(displayName: string): string {
    const parts = displayName.split(',').map((s) => s.trim()).filter(Boolean);
    if (parts.length <= 2) return displayName;
    return `${parts[0]}, ${parts[parts.length - 2]}`;
  }

  // Capitalize the first letter (for size-class labels etc.)
  function capitalize(s: string): string {
    return s.charAt(0).toUpperCase() + s.slice(1);
  }

  // Relative-frequency label for a species, scaled against the most-reported one here.
  function frequencyLabel(count: number, maxCount: number): { text: string; cls: string } {
    const ratio = maxCount > 0 ? count / maxCount : 0;
    if (ratio >= 0.5) return { text: 'Common here', cls: 'freq-common' };
    if (ratio >= 0.15) return { text: 'Regular here', cls: 'freq-regular' };
    if (ratio >= 0.04) return { text: 'Occasional', cls: 'freq-occasional' };
    return { text: 'Rare here', cls: 'freq-rare' };
  }

  function getColorForOrder(order: string): string {
    switch (order) {
      case 'Accipitriformes':
        return mapToken('--map-hawk', '#ef4444'); // Hawks, Eagles
      case 'Falconiformes':
        return mapToken('--map-falcon', '#f97316'); // Falcons
      case 'Strigiformes':
        return mapToken('--map-owl', '#3b82f6'); // Owls
      default:
        return mapToken('--map-other', '#6b7280');
    }
  }

  async function fetchWikipediaData(speciesName: string): Promise<{ extract: string; image: string; commonName: string }> {
    try {
      const title = speciesName.replace(/ /g, '_');
      const response = await fetch(
        `https://en.wikipedia.org/api/rest_v1/page/summary/${encodeURIComponent(title)}`
      );

      if (!response.ok) {
        return { extract: '', image: '', commonName: '' };
      }

      const data = await response.json();
      // Check if page title differs from scientific name and looks English
      const pageTitle = data.title || '';
      const isCommonName = pageTitle &&
        pageTitle.toLowerCase() !== speciesName.toLowerCase() &&
        /^[A-Za-z\s\-']+$/.test(pageTitle);

      // Require minimum 800px width — Wikipedia thumbnails can be very small
      const MIN_WIDTH = 800;
      const origSrc = data.originalimage?.source || '';
      const origWidth = data.originalimage?.width ?? 0;
      const thumbSrc = data.thumbnail?.source || '';
      const thumbWidth = data.thumbnail?.width ?? 0;
      const image = origWidth >= MIN_WIDTH ? origSrc
                  : thumbWidth >= MIN_WIDTH ? thumbSrc
                  : '';

      return {
        extract: data.extract || '',
        image,
        commonName: isCommonName ? pageTitle : '',
      };
    } catch {
      return { extract: '', image: '', commonName: '' };
    }
  }

  async function fetchINaturalistData(speciesName: string): Promise<{ photos: string[]; commonName: string }> {
    try {
      const response = await fetch(
        `https://api.inaturalist.org/v1/taxa?q=${encodeURIComponent(speciesName)}&rank=species&per_page=1&locale=en`
      );

      if (!response.ok) {
        return { photos: [], commonName: '' };
      }

      const data = await response.json();
      if (data.results && data.results.length > 0) {
        const taxon = data.results[0];
        // Gallery container is 3:2 landscape — pick the photo whose aspect ratio
        // is closest to that among the top 4 quality-ranked photos. Most bird
        // photography is already landscape, so this usually keeps the highest-quality
        // shot; it only swaps when the top photo is unusually portrait/square.
        const GALLERY_RATIO = 3 / 2;
        const rawPhotos: any[] = taxon.taxon_photos?.slice(0, 4) || [];
        const sortedPhotos = [...rawPhotos].sort((a, b) => {
          const ratio = (p: any) => {
            const w = p.photo.original_dimensions?.width || 1000;
            const h = p.photo.original_dimensions?.height || 1000;
            return w / h;
          };
          return Math.abs(ratio(a) - GALLERY_RATIO) - Math.abs(ratio(b) - GALLERY_RATIO);
        });
        const photos = sortedPhotos.map((p: any) => p.photo.large_url || p.photo.medium_url).filter(Boolean);
        // Use english_common_name or preferred_common_name if it looks English
        const commonName = taxon.english_common_name || taxon.preferred_common_name || '';
        const isEnglish = /^[A-Za-z\s\-']+$/.test(commonName);
        return {
          photos: photos.slice(0, 6),
          commonName: isEnglish ? commonName : '',
        };
      }

      return { photos: [], commonName: '' };
    } catch {
      return { photos: [], commonName: '' };
    }
  }

  // "How to spot it" — curated field marks + behavior + difficulty.
  function renderFieldNotes(raptor: RaptorInfo | null) {
    const el = document.getElementById('species-modal-fieldnotes')!;
    if (!raptor) {
      el.innerHTML = '';
      return;
    }
    const diffLabel: Record<string, string> = {
      easy: 'Easy to spot',
      moderate: 'Moderately tricky',
      tricky: 'A challenge to find & ID',
    };
    const cues = raptor.idCues.map((c) => `<li>${c}</li>`).join('');
    const fallbackNote = raptor.isFallback
      ? `<p class="fieldnotes-fallback">General notes for this group — species-specific tips aren't available for this one.</p>`
      : '';
    el.innerHTML = `
      <h4 class="species-block-title">How to spot it <span class="fieldnotes-diff">${diffLabel[raptor.difficulty]}</span></h4>
      ${fallbackNote}
      <ul class="fieldnotes-cues">${cues}</ul>
      <p class="fieldnotes-behavior">${raptor.behavior}</p>
    `;
  }

  // Wingspan visual: the species' range plotted against familiar reference points.
  function renderWingspanBar(raptor: RaptorInfo | null) {
    const el = document.getElementById('species-modal-wingspan')!;
    if (!raptor) {
      el.innerHTML = '';
      return;
    }
    const max = WINGSPAN_AXIS_MAX_CM;
    const [lo, hi] = raptor.wingspanCm;
    const leftPct = (lo / max) * 100;
    const widthPct = ((hi - lo) / max) * 100;
    const inLo = Math.round(lo / 2.54);
    const inHi = Math.round(hi / 2.54);

    // Reference points sit close together at the low end of the axis (40 and
    // 95 cm of 310), so on a narrow modal their labels would collide. Alternate
    // them between two heights — neighbours then never share a row.
    const refs = WINGSPAN_REFERENCES.map(
      (r, i) => `
      <div class="wingspan-ref${i % 2 === 1 ? ' wingspan-ref--raised' : ''}" style="left:${(r.cm / max) * 100}%">
        <span class="wingspan-ref-tick"></span>
        <span class="wingspan-ref-label">${r.label}<br>${r.cm} cm</span>
      </div>`
    ).join('');

    el.innerHTML = `
      <h4 class="species-block-title">Wingspan</h4>
      <div class="wingspan-track">
        <div class="wingspan-range" style="left:${leftPct}%;width:${widthPct}%"></div>
        ${refs}
      </div>
      <p class="species-block-caption">${lo}–${hi} cm (${inLo}–${inHi} in) tip to tip — a ${raptor.sizeClass} raptor.</p>
    `;
  }

  function formatPeaks(peaks: string[]): string {
    if (peaks.length === 0) return 'no particular month';
    if (peaks.length >= 6) return 'many months (fairly year-round)';
    if (peaks.length === 1) return peaks[0];
    return peaks.slice(0, -1).join(', ') + ' and ' + peaks[peaks.length - 1];
  }

  function closeSpeciesModal() {
    const modal = document.getElementById('species-modal')!;
    modal.style.display = 'none';
    document.body.style.overflow = '';
  }

  // Modal chrome is shared, so it is wired up once here; the per-instance code
  // only fills it in. The optional chaining keeps a map usable on a page that
  // forgot to render <BirdsSpeciesModal />.
  document.getElementById('species-modal-close')?.addEventListener('click', closeSpeciesModal);
  document.querySelector('.species-modal-backdrop')?.addEventListener('click', closeSpeciesModal);
  document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape') closeSpeciesModal();
  });

  /* ── Per-instance widget ── */

  // Each map root carries its own starting point, radius and place name as data
  // attributes, and owns its own Leaflet map, markers, cache and species data.
  function createBirdsMap(root: HTMLElement) {
    const q = (sel: string) => root.querySelector(sel) as HTMLElement;

    const DEFAULT_LAT = parseFloat(root.dataset.lat || '0');
    const DEFAULT_LNG = parseFloat(root.dataset.lng || '0');
    const DEFAULT_PLACE = root.dataset.place || 'this area';
    const DEFAULT_RADIUS = parseInt(root.dataset.radius || '100', 10);

    // Mutable search state — driven by the address box and radius slider
    let currentLat = DEFAULT_LAT;
    let currentLng = DEFAULT_LNG;
    let currentRadiusMiles = DEFAULT_RADIUS;
    let currentPlaceName = DEFAULT_PLACE;

    // Leaflet layers for the search center + radius ring (redrawn on every search)
    let centerMarker: any = null;
    let radiusCircle: any = null;

    let map: any;
    let markers: any[] = [];

    // The whole dataset for the current location, and the dot records expanded
    // out of it (payload dots are stored keyed by speciesKey to keep the JSON small).
    let payload: LocationPayload | null = null;
    let cachedOccurrences: any[] = [];

    // Species in the current taxon view, keyed by scientific name.
    let speciesData: Map<string, SpeciesInfo> = new Map();

    async function initMap() {
      const L = await loadLeaflet();

      // Initialize map centered on this instance's default location
      map = L.map(q('.birds-map')).setView([currentLat, currentLng], 8);

      // Basemap follows the theme — standard OSM tiles glare badly against the
      // dark ground, so the dark theme uses CARTO's dark raster (no API key).
      const tiles =
        mapToken('--map-tiles', 'dark') === 'light'
          ? {
              url: 'https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png',
              attribution: '&copy; OpenStreetMap contributors',
            }
          : {
              url: 'https://{s}.basemaps.cartocdn.com/dark_all/{z}/{x}/{y}{r}.png',
              attribution: '&copy; OpenStreetMap contributors &copy; CARTO',
            };

      L.tileLayer(tiles.url, {
        attribution: tiles.attribution,
        maxZoom: 18,
      }).addTo(map);

      redrawCenter(false);
      updateHeading();

      // Load the default location (from cache if present, else fetch)
      await runAnalysis(false);

      wireControls();
    }

    // Draw / redraw the search-center marker and the radius ring for the current state.
    function redrawCenter(fitBounds: boolean) {
      const L = (window as any).L;
      if (centerMarker) map.removeLayer(centerMarker);
      if (radiusCircle) map.removeLayer(radiusCircle);

      centerMarker = L.circleMarker([currentLat, currentLng], {
        radius: 7,
        fillColor: mapToken('--map-center', '#0a0a0a'),
        color: mapToken('--map-center', '#0a0a0a'),
        weight: 2,
        opacity: 1,
        fillOpacity: 0.9,
      })
        .addTo(map)
        .bindPopup(`<strong>${currentPlaceName}</strong><br>Search center`);

      radiusCircle = L.circle([currentLat, currentLng], {
        radius: currentRadiusMiles * MILES_TO_KM * 1000,
        color: mapToken('--map-radius', '#737373'),
        fillColor: mapToken('--map-radius', '#737373'),
        fillOpacity: 0.04,
        weight: 1,
        dashArray: '4, 6',
      }).addTo(map);

      if (fitBounds) {
        map.flyToBounds(radiusCircle.getBounds(), { padding: [24, 24], duration: 0.6 });
      }
    }

    function updateHeading() {
      const heading = q('.birds-heading');
      const subtitle = q('.birds-subtitle');
      if (heading) heading.textContent = `Birds of Prey Near ${currentPlaceName}`;
      if (subtitle) {
        subtitle.textContent = `Recent sightings within ${currentRadiusMiles} miles of ${currentPlaceName} — from GBIF`;
      }
    }

    // Run the analysis for the current location/radius: use cache when available, else fetch.
    // Expand the compact payload dots into the shape the map + modal expect.
    function expandOccurrences(p: LocationPayload) {
      const byKey = new Map(p.species.map((sp) => [sp.key, sp]));
      cachedOccurrences = p.occurrences
        .map((o) => {
          const sp = byKey.get(o.k);
          if (!sp) return null;
          return {
            decimalLatitude: o.y,
            decimalLongitude: o.x,
            species: sp.name,
            order: sp.order,
            family: sp.family,
            eventDate: o.d,
            stateProvince: o.s,
            speciesKey: sp.key,
          };
        })
        .filter(Boolean);
    }

    function applyPayload(p: LocationPayload) {
      payload = p;
      expandOccurrences(p);
      displayBirdData((q('.birds-taxon') as HTMLSelectElement).value);
    }

    // Load the data for the current location. The default locations ship as a
    // baked JSON file, so the common case is one CDN request and no GBIF traffic
    // at all; anything else (a searched address, a changed radius, a forced
    // refresh) falls through to a live fetch.
    async function runAnalysis(forceFetch: boolean) {
      updateHeading();
      const elLoading = q('.birds-loading')!;
      const elError = q('.birds-error')!;
      elError.style.display = 'none';

      const seedUrl = root.dataset.seed;
      const isDefaultView =
        !!seedUrl &&
        currentLat === DEFAULT_LAT &&
        currentLng === DEFAULT_LNG &&
        currentRadiusMiles === DEFAULT_RADIUS;

      if (isDefaultView && !forceFetch) {
        try {
          const res = await fetch(seedUrl!);
          if (res.ok) {
            const seed = (await res.json()) as LocationPayload;
            applyPayload(seed);
            setDataAge(Date.parse(seed.fetchedAt), 'baked');
            return;
          }
        } catch {
          // Fall through to the live path — a missing seed file must not be fatal.
        }
      }

      const key = cacheKey(currentLat, currentLng, currentRadiusMiles);
      if (!forceFetch) {
        const cached = await loadFromCache(key);
        if (cached?.payload?.version === 2) {
          applyPayload(cached.payload);
          setDataAge(cached.timestamp, 'cached');
          return;
        }
      }

      elLoading.style.display = 'block';
      elLoading.textContent = 'Looking up birds of prey…';
      try {
        const fresh = await fetchLocationData({
          lat: currentLat,
          lng: currentLng,
          radiusMiles: currentRadiusMiles,
          place: currentPlaceName,
          samplePages: 1,
          concurrency: 4,
          onProgress: (done: number, total: number) => {
            elLoading.textContent = `Looking up birds of prey… ${done}/${total}`;
          },
        });

        applyPayload(fresh);
        const savedAt = await saveToCache(key, fresh);
        setDataAge(savedAt || Date.now(), 'live');

        // Common names are the slow part, so they land after the first paint and
        // relabel the cards in place rather than holding the map hostage.
        enrichVernacularNames(fresh.species, { concurrency: 6 }).then(() => {
          if (payload === fresh) {
            applyPayload(fresh);
            saveToCache(key, fresh);
          }
        });
      } catch (err) {
        console.error(err);
        showError();
      }
    }

    function showError() {
      const elError = q('.birds-error')!;
      q('.birds-loading')!.style.display = 'none';
      elError.style.display = 'block';
      elError.textContent =
        'Bird data is temporarily unavailable (the GBIF service may be busy). Please try again in a moment.';

      markers.forEach((m) => map.removeLayer(m));
      markers = [];
      payload = null;
      cachedOccurrences = [];
      speciesData.clear();
      q('.birds-species-grid')!.innerHTML = '';
      q('.birds-insights')!.style.display = 'none';
      q('.birds-count')!.textContent = '0 sightings';
      q('.birds-species-count')!.textContent = '0 species';
    }

    function wireControls() {
      // Address search
      const form = q('.birds-search-form') as HTMLFormElement;
      const input = q('.birds-address-input') as HTMLInputElement;
      const searchBtn = q('.birds-search-btn') as HTMLButtonElement;
      const placeEl = q('.birds-place')!;

      form.addEventListener('submit', async (e) => {
        e.preventDefault();
        const q = input.value.trim();
        if (!q) return;

        placeEl.textContent = 'Finding location…';
        placeEl.className = 'birds-place';
        searchBtn.disabled = true;

        try {
          const geo = await geocodeAddress(q);
          if (!geo) {
            placeEl.textContent = `Couldn't find "${q}". Try a city, ZIP code, or a more specific address.`;
            placeEl.className = 'birds-place birds-place-error';
            return;
          }
          currentLat = geo.lat;
          currentLng = geo.lng;
          currentPlaceName = shortPlaceName(geo.displayName);
          placeEl.innerHTML = `Showing results within ${currentRadiusMiles} mi of <strong>${geo.displayName}</strong>`;
          placeEl.className = 'birds-place';
          redrawCenter(true);
          await runAnalysis(false);
        } catch (err) {
          console.error(err);
          placeEl.textContent = 'Location lookup failed. Please check your connection and try again.';
          placeEl.className = 'birds-place birds-place-error';
        } finally {
          searchBtn.disabled = false;
        }
      });

      // Radius slider — update the label live, re-run on release (debounced)
      const slider = q('.birds-radius-slider') as HTMLInputElement;
      const radiusValue = q('.birds-radius-value')!;
      let radiusTimer: number | undefined;

      slider.addEventListener('input', () => {
        radiusValue.textContent = slider.value;
      });

      slider.addEventListener('change', () => {
        currentRadiusMiles = parseInt(slider.value, 10);
        redrawCenter(true);
        window.clearTimeout(radiusTimer);
        radiusTimer = window.setTimeout(() => runAnalysis(false), 250);
      });

      // Taxon filter — re-render from cache
      q('.birds-taxon')!.addEventListener('change', (e) => {
        const value = (e.target as HTMLSelectElement).value;
        displayBirdData(value);
      });

      // Force refresh from the API for the current location
      q('.birds-refresh')!.addEventListener('click', async () => {
        await runAnalysis(true);
      });

      // "View Species Details" inside a Leaflet popup. Delegated from the map
      // element so the click resolves against this instance's species data.
      q('.birds-map').addEventListener('click', (e) => {
        const btn = (e.target as HTMLElement).closest('.popup-species-btn') as HTMLElement | null;
        if (btn?.dataset.species) openSpeciesModal(btn.dataset.species);
      });
    }

    // Render the current taxon view.
    //
    // Counts come from the payload's species rows, which are facet-derived and
    // cover the whole dataset. The markers are only the sample, so the two
    // numbers differ by orders of magnitude — the stats bar says so explicitly.
    function displayBirdData(taxonFilter: string) {
      const L = (window as any).L;
      q('.birds-loading')!.style.display = 'none';
      const elCount = q('.birds-count')!;
      const elSpecies = q('.birds-species-count')!;
      const elSample = q('.birds-sample-note')!;

      markers.forEach((m) => map.removeLayer(m));
      markers = [];
      speciesData.clear();

      if (!payload || payload.species.length === 0) {
        elCount.textContent = '0 sightings';
        elSpecies.textContent = '0 species';
        elSample.textContent = '';
        q('.birds-species-grid')!.innerHTML = '';
        q('.birds-insights')!.style.display = 'none';
        return;
      }

      const inView = (order: string) => taxonFilter === 'all' || order === taxonFilter;

      let totalSightings = 0;
      for (const sp of payload.species) {
        if (!inView(sp.order)) continue;
        totalSightings += sp.count;
        speciesData.set(sp.name, {
          scientificName: sp.name,
          vernacularName: sp.vernacular,
          order: sp.order,
          family: sp.family,
          count: sp.count,
          gbifKey: sp.key,
        });
      }

      for (const occ of cachedOccurrences) {
        if (!inView(occ.order)) continue;

        const marker = L.circleMarker([occ.decimalLatitude, occ.decimalLongitude], {
          radius: 6,
          fillColor: getColorForOrder(occ.order),
          color: mapToken('--map-marker-stroke', '#fff'),
          weight: 1,
          opacity: 1,
          fillOpacity: 0.8,
        });

        const date = occ.eventDate ? new Date(occ.eventDate).toLocaleDateString() : 'Date unknown';

        marker.bindPopup(`
        <strong>${occ.species}</strong><br>
        Order: ${occ.order}<br>
        Date: ${date}<br>
        ${occ.stateProvince ? `Location: ${occ.stateProvince}<br>` : ''}
        <button class="popup-species-btn" data-species="${escapeAttr(occ.species)}">View Species Details</button>
      `);

        marker.addTo(map);
        markers.push(marker);
      }

      renderSpeciesList();
      renderInsights(totalSightings);

      elCount.textContent = `${totalSightings.toLocaleString()} sightings`;
      elSpecies.textContent = `${speciesData.size} species`;
      elSample.textContent = markers.length
        ? `map shows a ${markers.length.toLocaleString()}-sighting sample`
        : '';
    }

    // Stamp where the data came from and how old it is.
    function setDataAge(timestamp: number, source: 'baked' | 'cached' | 'live') {
      const el = q('.birds-cache-status');
      if (!el || !timestamp) return;
      const when = new Date(timestamp).toLocaleDateString('en-US', {
        month: 'short',
        day: 'numeric',
        year: 'numeric',
      });
      el.textContent = source === 'live' ? 'Updated just now' : `GBIF data from ${when}`;
      el.classList.toggle('fresh', source === 'live');
    }

    // Plain-language summary panel above the species grid.
    function renderInsights(totalSightings: number) {
      const el = q('.birds-insights');
      if (!el) return;
      el.style.display = 'block';

      const entries = Array.from(speciesData.entries())
        .filter(([name, info]) => name !== 'Unknown' && info.order !== 'Unknown')
        .sort((a, b) => b[1].count - a[1].count);

      if (entries.length === 0) {
        el.innerHTML = `<p class="birds-insights-lead">No birds of prey were reported within ${currentRadiusMiles} miles of <strong>${currentPlaceName}</strong> in the last five years. Try widening the radius or searching a different place.</p>`;
        return;
      }

      const nameOf = ([name, info]: [string, SpeciesInfo]) => info.vernacularName || name;
      const top = entries[0];
      const rare = entries[entries.length - 1];

      const orders: Record<string, number> = { Accipitriformes: 0, Falconiformes: 0, Strigiformes: 0 };
      entries.forEach(([, info]) => {
        if (orders[info.order] !== undefined) orders[info.order]++;
      });

      // Only call out a "rare find" when it's genuinely uncommon relative to the top species.
      const showRare = rare[0] !== top[0] && rare[1].count <= Math.max(2, top[1].count * 0.1);
      const rareSentence = showRare
        ? ` A rarer find here is <strong>${nameOf(rare)}</strong>, with just ${rare[1].count.toLocaleString()} ${rare[1].count === 1 ? 'report' : 'reports'}.`
        : '';

      el.innerHTML = `
        <div class="birds-insights-grid">
          <div class="insight-stat"><span class="insight-num">${entries.length}</span><span class="insight-label">species</span></div>
          <div class="insight-stat"><span class="insight-num">${totalSightings.toLocaleString()}</span><span class="insight-label">sightings</span></div>
          <div class="insight-stat"><span class="insight-num">${currentRadiusMiles}</span><span class="insight-label">mile radius</span></div>
        </div>
        <p class="birds-insights-lead">
          Within ${currentRadiusMiles} miles of <strong>${currentPlaceName}</strong>, observers have logged
          <strong>${entries.length}</strong> ${entries.length === 1 ? 'species' : 'species'} of birds of prey.
          The one you're most likely to see is <strong>${nameOf(top)}</strong>
          (${top[1].count} ${top[1].count === 1 ? 'report' : 'reports'}).${rareSentence}
        </p>
        <div class="birds-insights-breakdown">
          <span><span class="dot-inline" style="background:${getColorForOrder('Accipitriformes')}"></span> ${orders.Accipitriformes} hawk &amp; eagle ${orders.Accipitriformes === 1 ? 'species' : 'species'}</span>
          <span><span class="dot-inline" style="background:${getColorForOrder('Falconiformes')}"></span> ${orders.Falconiformes} falcon ${orders.Falconiformes === 1 ? 'species' : 'species'}</span>
          <span><span class="dot-inline" style="background:${getColorForOrder('Strigiformes')}"></span> ${orders.Strigiformes} owl ${orders.Strigiformes === 1 ? 'species' : 'species'}</span>
        </div>
      `;
    }

    /* ── Species Grid ── */
    function renderSpeciesList() {
      const gridEl = q('.birds-species-grid')!;
      const countLabel = q('.birds-species-count-label')!;
      gridEl.innerHTML = '';

      // Filter out unknown and sort by count descending
      const filtered = Array.from(speciesData.entries())
        .filter(([name, info]) => name !== 'Unknown' && info.order !== 'Unknown')
        .sort((a, b) => b[1].count - a[1].count);

      countLabel.textContent = `(${filtered.length})`;

      const maxCount = filtered.length > 0 ? filtered[0][1].count : 0;

      for (const [name, info] of filtered) {
        const displayName = info.vernacularName || name;
        const raptor = getRaptorInfo(name, info.family);
        const freq = frequencyLabel(info.count, maxCount);
        const sizeText = raptor
          ? `${capitalize(raptor.sizeClass)} · ${raptor.wingspanCm[0]}–${raptor.wingspanCm[1]} cm wingspan`
          : '';

        const card = document.createElement('div');
        card.className = 'species-card';
        card.innerHTML = `
          <div class="species-card-color" style="background: ${getColorForOrder(info.order)}"></div>
          <div class="species-card-content">
            <span class="species-card-common">${displayName}</span>
            <span class="species-card-scientific">${name}</span>
            ${sizeText ? `<span class="species-card-size">${sizeText}</span>` : ''}
          </div>
          <div class="species-card-meta">
            <span class="species-freq ${freq.cls}">${freq.text}</span>
            <span class="species-card-count">${info.count.toLocaleString()} ${info.count === 1 ? 'report' : 'reports'}</span>
          </div>
        `;
        card.addEventListener('click', () => openSpeciesModal(name));
        gridEl.appendChild(card);
      }
    }

    /* ── Species Modal ── */
    async function openSpeciesModal(speciesName: string) {
      const modal = document.getElementById('species-modal')!;
      const loading = document.getElementById('species-modal-loading')!;
      const body = document.getElementById('species-modal-body')!;

      modal.style.display = 'flex';
      document.body.style.overflow = 'hidden';
      loading.style.display = 'block';
      body.style.display = 'none';

      const info = speciesData.get(speciesName);
      if (!info) {
        loading.textContent = 'Species not found';
        return;
      }

      try {
        // Fetch data from both APIs in parallel
        const [wikiData, inatData] = await Promise.all([
          fetchWikipediaData(speciesName),
          fetchINaturalistData(speciesName),
        ]);

        // Populate modal
        document.getElementById('species-modal-name')!.textContent = speciesName;
        document.getElementById('species-modal-common')!.textContent = info.vernacularName || wikiData.commonName || inatData.commonName || '';
        document.getElementById('species-modal-order')!.textContent = info.order;
        document.getElementById('species-modal-family')!.textContent = info.family;
        document.getElementById('species-modal-sightings')!.textContent = `${info.count.toLocaleString()} sightings`;

        // Description from Wikipedia
        const descEl = document.getElementById('species-modal-description')!;
        descEl.innerHTML = wikiData.extract || '<em>No description available.</em>';

        // Curated enrichment: how-to-spot notes, wingspan visual, seasonality
        const raptor = getRaptorInfo(speciesName, info.family);
        renderFieldNotes(raptor);
        renderWingspanBar(raptor);
        renderSeasonality(speciesName);

        // Images - prefer iNaturalist, fallback to Wikipedia
        const mainImage = document.getElementById('species-modal-image') as HTMLImageElement;
        const thumbnails = document.getElementById('species-modal-thumbnails')!;
        thumbnails.innerHTML = '';

        // iNaturalist large photos lead — Wikipedia appended as supplement only
        const allPhotos = [...inatData.photos];
        if (wikiData.image && !allPhotos.includes(wikiData.image)) {
          allPhotos.push(wikiData.image);
        }

        if (allPhotos.length > 0) {
          mainImage.src = allPhotos[0];
          mainImage.alt = speciesName;
          mainImage.style.display = 'block';

          // Add thumbnails if multiple photos
          if (allPhotos.length > 1) {
            allPhotos.slice(0, 6).forEach((url, idx) => {
              const thumb = document.createElement('img');
              thumb.src = url;
              thumb.alt = `${speciesName} photo ${idx + 1}`;
              thumb.className = idx === 0 ? 'active' : '';
              thumb.addEventListener('click', () => {
                mainImage.src = url;
                thumbnails.querySelectorAll('img').forEach(t => t.classList.remove('active'));
                thumb.classList.add('active');
              });
              thumbnails.appendChild(thumb);
            });
          }
        } else {
          mainImage.style.display = 'none';
        }

        // Links
        const wikiTitle = speciesName.replace(/ /g, '_');
        (document.getElementById('species-link-wikipedia') as HTMLAnchorElement).href =
          `https://en.wikipedia.org/wiki/${wikiTitle}`;
        (document.getElementById('species-link-inaturalist') as HTMLAnchorElement).href =
          `https://www.inaturalist.org/taxa/search?q=${encodeURIComponent(speciesName)}`;
        (document.getElementById('species-link-gbif') as HTMLAnchorElement).href =
          `https://www.gbif.org/species/${info.gbifKey}`;

        loading.style.display = 'none';
        body.style.display = 'flex';
      } catch (err) {
        console.error('Error loading species data:', err);
        loading.textContent = 'Failed to load species information.';
      }
    }

    // Seasonality: when this species is reported near the current location.
    function renderSeasonality(speciesName: string) {
      const el = document.getElementById('species-modal-seasonality')!;
      const months = new Array(12).fill(0);
      let dated = 0;

      for (const occ of cachedOccurrences) {
        const nm = occ.species || occ.genericName;
        if (nm !== speciesName || !occ.eventDate) continue;
        const m = new Date(occ.eventDate).getMonth();
        if (!isNaN(m)) {
          months[m]++;
          dated++;
        }
      }

      // Too few dated records to say anything meaningful.
      if (dated < 4) {
        el.innerHTML = '';
        return;
      }

      const max = Math.max(...months);
      const short = ['J', 'F', 'M', 'A', 'M', 'J', 'J', 'A', 'S', 'O', 'N', 'D'];
      const full = ['January', 'February', 'March', 'April', 'May', 'June', 'July', 'August', 'September', 'October', 'November', 'December'];

      const peaks = months
        .map((v, i) => ({ v, i }))
        .filter((o) => o.v > 0 && o.v >= max * 0.85)
        .map((o) => full[o.i]);

      const bars = months
        .map(
          (v, i) => `
          <div class="season-bar-wrap" title="${full[i]}: ${v} ${v === 1 ? 'report' : 'reports'}">
            <div class="season-bar" style="height:${max ? (v / max) * 100 : 0}%"></div>
            <span class="season-bar-label">${short[i]}</span>
          </div>`
        )
        .join('');

      el.innerHTML = `
        <h4 class="species-block-title">When it's reported near ${currentPlaceName}</h4>
        <div class="season-chart">${bars}</div>
        <p class="species-block-caption">Most reported in ${formatPeaks(peaks)}. Based on ${dated} dated ${dated === 1 ? 'sighting' : 'sightings'} — counts reflect when people report birds, not only when the birds are present.</p>
      `;
    }

    initMap();
  }

  document
    .querySelectorAll<HTMLElement>('[data-birds-map]')
    .forEach((root) => createBirdsMap(root));
</script>
<style is:global>
  .birds-map-container {
    margin: 2rem 0;
  }

  .birds-header {
    margin-bottom: 1.25rem;
    border-top: 1px solid var(--color-border);
    padding-top: 1.5rem;
  }

  .birds-header h2 {
    font-family: var(--font-display);
    font-size: 1.75rem;
    font-weight: 600;
    letter-spacing: -0.01em;
    margin-bottom: 0.25rem;
  }

  .birds-subtitle {
    color: var(--color-muted);
    font-size: 0.8125rem;
    letter-spacing: 0.04em;
  }

  .birds-controls {
    display: flex;
    gap: 0.5rem;
    margin-bottom: 1rem;
    flex-wrap: wrap;
  }

  .birds-taxon {
    padding: 0.5rem 0.75rem;
    border: 1px solid var(--color-border);
    border-radius: 0;
    font-size: 0.875rem;
    font-family: var(--font-body);
    background: var(--color-surface);
    color: var(--color-text);
    cursor: pointer;
    min-width: 200px;
    appearance: auto;
  }

  .birds-taxon:focus {
    outline: none;
    border-color: var(--color-text);
  }

  .birds-refresh {
    padding: 0.5rem 1rem;
    background: var(--sec, var(--color-accent));
    color: var(--color-bg);
    border: 1px solid var(--sec, var(--color-accent));
    border-radius: 0;
    font-size: 0.75rem;
    font-family: var(--font-body);
    letter-spacing: 0.08em;
    text-transform: uppercase;
    cursor: pointer;
    transition: background-color 0.15s, border-color 0.15s;
  }

  .birds-refresh:hover {
    background: var(--sec, var(--color-accent-hover));
    border-color: var(--sec, var(--color-accent-hover));
  }

  /* Address search */
  .birds-search {
    display: flex;
    gap: 0.5rem;
    margin-bottom: 0.75rem;
  }

  .birds-address-input {
    flex: 1;
    min-width: 0;
    padding: 0.5rem 0.75rem;
    border: 1px solid var(--color-border);
    border-radius: 0;
    font-size: 0.875rem;
    font-family: var(--font-body);
    background: var(--color-surface);
    color: var(--color-text);
  }

  .birds-address-input:focus {
    outline: none;
    border-color: var(--color-text);
  }

  .birds-search-btn {
    padding: 0.5rem 1.25rem;
    background: var(--sec, var(--color-accent));
    color: var(--color-bg);
    border: 1px solid var(--sec, var(--color-accent));
    border-radius: 0;
    font-size: 0.75rem;
    font-family: var(--font-body);
    letter-spacing: 0.08em;
    text-transform: uppercase;
    cursor: pointer;
    transition: background-color 0.15s, border-color 0.15s;
  }

  .birds-search-btn:hover {
    background: var(--sec, var(--color-accent-hover));
    border-color: var(--sec, var(--color-accent-hover));
  }

  .birds-search-btn:disabled {
    opacity: 0.5;
    cursor: default;
  }

  /* Radius slider */
  .birds-radius {
    display: flex;
    flex-direction: column;
    gap: 0.4rem;
    margin-bottom: 0.75rem;
  }

  .birds-radius label {
    font-size: 0.8125rem;
    color: var(--color-muted);
    letter-spacing: 0.02em;
  }

  .birds-radius label .birds-radius-value {
    color: var(--color-text);
    font-weight: 500;
  }

  .birds-radius-slider {
    width: 100%;
    max-width: 340px;
    accent-color: var(--sec, var(--color-accent));
    cursor: pointer;
  }

  /* Resolved-location / error message */
  .birds-place {
    font-size: 0.8125rem;
    line-height: 1.5;
    color: var(--color-muted);
    margin-bottom: 1rem;
    min-height: 1.2em;
  }

  .birds-place strong {
    color: var(--color-text);
    font-weight: 500;
  }

  .birds-place-error {
    color: var(--color-error);
  }

  /* Area insights panel */
  .birds-insights {
    margin-top: 1.25rem;
    padding: 1.25rem 1.5rem;
    border: 1px solid var(--color-border);
    background: var(--color-surface);
  }

  .birds-insights-grid {
    display: flex;
    gap: 2rem;
    margin-bottom: 1rem;
    flex-wrap: wrap;
  }

  .insight-stat {
    display: flex;
    flex-direction: column;
    line-height: 1.1;
  }

  .insight-num {
    font-family: var(--font-display);
    font-size: 2rem;
    font-weight: 600;
    color: var(--color-text);
  }

  .insight-label {
    font-size: 0.6875rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--color-muted);
    margin-top: 0.2rem;
  }

  .birds-insights-lead {
    font-size: 0.9375rem;
    line-height: 1.7;
    color: var(--color-text);
    margin-bottom: 1rem;
  }

  .birds-insights-lead strong {
    font-weight: 600;
  }

  .birds-insights-breakdown {
    display: flex;
    flex-wrap: wrap;
    gap: 1.25rem;
    font-size: 0.8125rem;
    color: var(--color-muted);
    padding-top: 1rem;
    border-top: 1px solid var(--color-border);
  }

  .birds-insights-breakdown span {
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
  }

  .dot-inline {
    display: inline-block;
    width: 8px;
    height: 8px;
    border-radius: 50%;
    flex-shrink: 0;
  }

  .birds-message {
    padding: 2rem;
    text-align: center;
    border: 1px solid var(--color-border);
    color: var(--color-muted);
    font-size: 0.875rem;
  }

  .birds-error {
    border-color: var(--color-error-border);
    color: var(--color-error);
  }

  /* Framed so the light panel reads as a deliberate lit instrument inset into
     the dark page, rather than an untreated bright slab. */
  .birds-map {
    height: 600px;
    border: 1px solid var(--color-border);
    border-radius: 10px;
    box-shadow: var(--shadow-md);
    overflow: hidden;
  }

  /* Species Section */
  .birds-species-section {
    margin-top: 2rem;
    padding-top: 1.5rem;
    border-top: 1px solid var(--color-border);
  }

  .birds-species-section h3 {
    font-family: var(--font-display);
    font-size: 1.5rem;
    font-weight: 600;
    letter-spacing: -0.01em;
    margin-bottom: 0.25rem;
  }

  .birds-species-section h3 span {
    font-family: var(--font-body);
    font-weight: 400;
    color: var(--color-muted);
    font-size: 0.875rem;
    letter-spacing: 0;
  }

  .birds-section-subtitle {
    font-size: 0.8125rem;
    color: var(--color-muted);
    margin-bottom: 1.25rem;
    letter-spacing: 0.02em;
  }

  .birds-species-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
    gap: 1px;
    background: var(--color-border);
    border: 1px solid var(--color-border);
  }

  .species-card {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    padding: 0.875rem 1rem;
    background: var(--color-surface);
    cursor: pointer;
    transition: background 0.15s;
  }

  .species-card:hover {
    background: var(--color-surface-2);
  }

  .species-card-color {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    flex-shrink: 0;
  }

  .species-card-content {
    flex: 1;
    min-width: 0;
  }

  .species-card-common {
    display: block;
    font-weight: 500;
    font-size: 0.9375rem;
    color: var(--color-text);
    margin-bottom: 0.125rem;
  }

  .species-card-scientific {
    display: block;
    font-size: 0.8125rem;
    color: var(--color-muted);
    font-style: italic;
  }

  .species-card-size {
    display: block;
    font-size: 0.75rem;
    color: var(--color-muted);
    margin-top: 0.25rem;
  }

  .species-card-meta {
    display: flex;
    flex-direction: column;
    align-items: flex-end;
    gap: 0.35rem;
    flex-shrink: 0;
  }

  .species-card-count {
    font-size: 0.6875rem;
    font-weight: 500;
    color: var(--color-muted);
    letter-spacing: 0.04em;
    flex-shrink: 0;
    white-space: nowrap;
  }

  .species-freq {
    font-size: 0.625rem;
    font-weight: 500;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    padding: 0.15rem 0.4rem;
    border: 1px solid currentColor;
    white-space: nowrap;
  }

  .species-freq.freq-common { color: var(--freq-common); }
  .species-freq.freq-regular { color: var(--freq-regular); }
  .species-freq.freq-occasional { color: var(--freq-occasional); }
  .species-freq.freq-rare { color: var(--freq-rare); }

  /* Popup button */
  .popup-species-btn {
    display: block;
    width: 100%;
    margin-top: 0.5rem;
    padding: 0.375rem 0.5rem;
    background: var(--sec, var(--color-accent));
    color: var(--color-bg);
    border: none;
    font-size: 0.75rem;
    font-family: var(--font-body);
    letter-spacing: 0.06em;
    text-transform: uppercase;
    cursor: pointer;
    transition: opacity 0.15s;
  }

  .popup-species-btn:hover {
    opacity: 0.75;
  }

  .birds-stats {
    display: flex;
    gap: 1.5rem;
    margin-top: 1rem;
    font-size: 0.8125rem;
    color: var(--color-muted);
    letter-spacing: 0.04em;
  }

  .birds-stats span {
    padding: 0;
    background: transparent;
    font-weight: 400;
  }

  /* The sightings figure covers the whole dataset while the dots are a sample —
     this note carries that distinction, so it sits apart from the two stats. */
  .birds-sample-note {
    color: var(--color-muted);
    opacity: 0.75;
    font-style: italic;
  }

  .cache-status {
    font-size: 0.75rem;
    color: var(--color-muted);
  }

  .cache-status.fresh {
    color: var(--color-ok);
  }

  .birds-attribution {
    margin-top: 1rem;
    font-size: 0.75rem;
    color: var(--color-muted);
    letter-spacing: 0.02em;
  }

  .birds-attribution a {
    color: var(--color-text);
    text-decoration: underline;
    text-underline-offset: 3px;
    text-decoration-color: var(--color-border);
    transition: text-decoration-color 0.15s;
  }

  .birds-attribution a:hover {
    text-decoration-color: var(--color-text);
  }

  /* Leaflet popup styling */
  .leaflet-popup-content-wrapper {
    border-radius: 0;
    box-shadow: var(--shadow-md);
  }

  /* The map keeps its own light world — see the --map-* tokens in BaseLayout.
     Leaflet's chrome follows the basemap, not the page, so popups and controls
     stay legible against the tiles instead of matching the dark ground. */
  .leaflet-container {
    background: var(--map-panel);
  }

  .leaflet-popup-content-wrapper,
  .leaflet-popup-tip {
    background: var(--map-panel);
    color: var(--map-panel-text);
    border: 1px solid var(--map-panel-border);
  }

  .leaflet-popup-content {
    color: var(--map-panel-text);
  }

  .leaflet-bar a {
    background: var(--map-panel);
    color: var(--map-panel-text);
    border-bottom-color: var(--map-panel-border);
  }

  .leaflet-control-attribution {
    color: var(--map-panel-text);
  }

  .leaflet-control-attribution a {
    color: var(--color-muted);
  }

  .leaflet-popup-content {
    font-family: var(--font-body);
    font-size: 0.875rem;
    line-height: 1.5;
  }

  .leaflet-popup-content a {
    color: var(--color-text);
    text-decoration: underline;
  }

  /* Species Modal */
  .species-modal {
    display: none;
    position: fixed;
    inset: 0;
    z-index: 1000;
    align-items: center;
    justify-content: center;
    padding: 1rem;
  }

  .species-modal-backdrop {
    position: absolute;
    inset: 0;
    background: rgba(5, 9, 12, 0.78);
  }

  .species-modal-content {
    position: relative;
    background: var(--color-surface);
    border-radius: 0;
    width: 100%;
    max-width: 1100px;
    max-height: 92vh;
    overflow-y: auto;
    box-shadow: var(--shadow-lg);
    animation: modalIn 0.2s ease-out;
  }

  @keyframes modalIn {
    from { opacity: 0; transform: translateY(12px); }
    to   { opacity: 1; transform: translateY(0); }
  }

  .species-modal-close {
    position: absolute;
    top: 1rem;
    right: 1rem;
    z-index: 10;
    background: var(--color-surface);
    border: none;
    font-size: 1.5rem;
    cursor: pointer;
    color: var(--color-muted);
    width: 2rem;
    height: 2rem;
    display: flex;
    align-items: center;
    justify-content: center;
    line-height: 1;
    transition: color 0.15s;
  }

  .species-modal-close:hover {
    color: var(--color-text);
  }

  .species-modal-loading {
    padding: 4rem 2rem;
    text-align: center;
    color: var(--color-muted);
    font-size: 0.875rem;
  }

  .species-modal-body {
    display: flex;
    flex-direction: column;
  }

  .species-modal-gallery {
    width: 100%;
    aspect-ratio: 3 / 2;
    background: var(--color-surface);
    display: flex;
    flex-direction: column;
    flex-shrink: 0;
    position: relative;
  }

  #species-modal-image {
    flex: 1;
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center;
    min-height: 0;
  }

  .species-modal-thumbnails {
    display: flex;
    gap: 2px;
    padding: 2px;
    background: var(--color-surface);
    overflow-x: auto;
  }

  .species-modal-thumbnails img {
    width: 56px;
    height: 56px;
    object-fit: contain;
    cursor: pointer;
    opacity: 0.5;
    transition: opacity 0.15s;
  }

  .species-modal-thumbnails img:hover,
  .species-modal-thumbnails img.active {
    opacity: 1;
  }

  .species-modal-info {
    width: 100%;
    padding: 2.5rem 3rem;
  }

  #species-modal-name {
    font-family: var(--font-display);
    font-size: 2.25rem;
    font-weight: 600;
    letter-spacing: -0.01em;
    margin-bottom: 0.25rem;
    font-style: italic;
  }

  .species-common-name {
    font-size: 0.9375rem;
    color: var(--color-muted);
    margin-bottom: 1.25rem;
    letter-spacing: 0.02em;
  }

  .species-meta {
    display: flex;
    flex-wrap: wrap;
    gap: 0.375rem;
    margin-bottom: 1.5rem;
  }

  .species-tag {
    padding: 0.2rem 0.5rem;
    border: 1px solid var(--color-border);
    font-size: 0.6875rem;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    color: var(--color-muted);
  }

  .species-description {
    font-size: 0.9375rem;
    line-height: 1.75;
    color: var(--color-text);
    margin-bottom: 1.5rem;
  }

  /* Modal enrichment blocks (field notes / wingspan / seasonality) */
  .species-block-title {
    font-size: 0.6875rem;
    font-weight: 400;
    text-transform: uppercase;
    letter-spacing: 0.12em;
    color: var(--color-muted);
    margin-bottom: 0.75rem;
    display: flex;
    align-items: center;
    gap: 0.6rem;
  }

  .species-block-caption {
    font-size: 0.8125rem;
    line-height: 1.6;
    color: var(--color-muted);
    margin-top: 0.75rem;
  }

  .species-fieldnotes,
  .species-wingspan,
  .species-seasonality {
    margin-bottom: 1.75rem;
  }

  .fieldnotes-diff {
    font-size: 0.625rem;
    letter-spacing: 0.06em;
    padding: 0.15rem 0.45rem;
    border: 1px solid var(--color-border);
    color: var(--color-text);
    text-transform: none;
  }

  .fieldnotes-fallback {
    font-size: 0.8125rem;
    font-style: italic;
    color: var(--color-muted);
    margin-bottom: 0.6rem;
  }

  .fieldnotes-cues {
    list-style: none;
    padding: 0;
    margin: 0 0 0.75rem;
    display: flex;
    flex-direction: column;
    gap: 0.4rem;
  }

  .fieldnotes-cues li {
    position: relative;
    padding-left: 1.1rem;
    font-size: 0.9375rem;
    line-height: 1.5;
    color: var(--color-text);
  }

  .fieldnotes-cues li::before {
    content: '';
    position: absolute;
    left: 0;
    top: 0.5rem;
    width: 5px;
    height: 5px;
    border-radius: 50%;
    background: var(--sec, var(--color-accent));
  }

  .fieldnotes-behavior {
    font-size: 0.9375rem;
    line-height: 1.7;
    color: var(--color-text);
  }

  /* Wingspan visual */
  .wingspan-track {
    position: relative;
    height: 10px;
    background: var(--color-surface-2);
    /* Top margin clears two staggered rows of reference labels */
    margin: 6.25rem 0 0.5rem;
  }

  .wingspan-range {
    position: absolute;
    top: 0;
    height: 100%;
    background: var(--sec, var(--color-accent));
    min-width: 2px;
  }

  .wingspan-ref {
    position: absolute;
    top: 50%;
    transform: translate(-50%, -50%);
    display: flex;
    flex-direction: column;
    align-items: center;
    pointer-events: none;
  }

  .wingspan-ref-tick {
    width: 1px;
    height: 20px;
    background: var(--color-muted);
    opacity: 0.5;
  }

  .wingspan-ref-label {
    position: absolute;
    bottom: 16px;
    left: 50%;
    transform: translateX(-50%);
    /* A fixed, wrapping width — nowrap was what let long labels such as
       "Adult human, arms out" run into their neighbours on narrow screens. */
    width: 4.75rem;
    white-space: normal;
    font-size: 0.625rem;
    line-height: 1.25;
    color: var(--color-muted);
    text-align: center;
  }

  /* Offset enough that a three-line lower label (the longest reference wraps
     to three lines on very narrow screens) still clears the raised row. */
  .wingspan-ref--raised .wingspan-ref-label {
    bottom: 58px;
  }

  /* A hairline joins a raised label back to its tick so the pairing stays
     unambiguous once the rows are offset. */
  .wingspan-ref--raised .wingspan-ref-label::after {
    content: '';
    position: absolute;
    left: 50%;
    top: 100%;
    width: 1px;
    height: 38px;
    background: var(--color-muted);
    opacity: 0.3;
  }

  /* Seasonality bar strip */
  .season-chart {
    display: flex;
    align-items: flex-end;
    gap: 3px;
    height: 72px;
  }

  .season-bar-wrap {
    flex: 1;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: flex-end;
    height: 100%;
    gap: 0.3rem;
  }

  .season-bar {
    width: 100%;
    background: var(--sec, var(--color-accent));
    min-height: 1px;
    transition: height 0.2s;
  }

  .season-bar-label {
    font-size: 0.625rem;
    color: var(--color-muted);
  }

  .species-links {
    display: flex;
    gap: 0.5rem;
    padding-top: 1rem;
    border-top: 1px solid var(--color-border);
    flex-wrap: wrap;
  }

  .species-links a {
    padding: 0.375rem 0.75rem;
    border: 1px solid var(--color-border);
    font-size: 0.6875rem;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--color-muted);
    text-decoration: none;
    transition: border-color 0.15s, color 0.15s;
  }

  .species-links a:hover {
    border-color: var(--color-text);
    color: var(--color-text);
  }

  /* Responsive */
  @media (max-width: 900px) {
    .species-modal-info {
      padding: 1.5rem 1.25rem;
    }

    #species-modal-name {
      font-size: 1.75rem;
    }

    .birds-species-grid {
      grid-template-columns: repeat(auto-fill, minmax(240px, 1fr));
    }
  }

  @media (max-width: 640px) {
    .birds-map {
      height: 420px;
    }

    .birds-controls {
      flex-direction: column;
    }

    .birds-taxon {
      width: 100%;
    }

    .species-modal-content {
      max-height: 95vh;
    }

    .species-modal-info {
      padding: 1.25rem;
    }

    #species-modal-name {
      font-size: 1.5rem;
    }
  }
</style>
